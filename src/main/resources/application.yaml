user:
  bot:
    name:
    token:
admin:
  bot:
    name:
    token:
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quest_bot_db
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never